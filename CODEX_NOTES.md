# Codex 项目记忆

这个文件是给 Codex 后续接着改项目时看的。用户主要用这个工具箱跑梦幻西游手游的 scrcpy/ADB 自动化，核心目标是稳定、可编辑、少手工干预。

## 项目位置

- 项目目录：`C:\Users\tjs\Documents\脚本\scrcpy_human_automation`
- 启动工具箱：`启动工具箱.bat`，或运行 `.venv\Scripts\pythonw.exe toolbox.py`
- ADB 优先路径：`D:\soft\platform-tools\adb.exe`
- scrcpy 路径：`D:\soft\scrcpy\scrcpy.exe`
- 设备序列号：`6dce9cdd`
- 手机分辨率/截图横屏：`2608x1200`

## 主要文件

- `toolbox.py`：Tkinter 工具箱 UI，深色主题，默认隐藏左侧手机预览。
- `src/scrcpy_human_automation/runner.py`：流程执行器，处理点击、模板识别、OCR、战斗等待、条件步骤。
- `src/scrcpy_human_automation/device.py`：ADB 包装，截图用 `exec-out screencap -p`，后台可识别，不依赖 scrcpy 窗口在最前。
- `src/scrcpy_human_automation/vision.py`：OpenCV 模板匹配。
- `flows/钟馗抓鬼.json`：当前重点流程。
- `flows/厢房.json`：厢房流程。
- `templates/`：模板库。
- `logs/`：运行日志。
- `failures/`：失败截图和 debug 截图。
- `state/zhongkui_ghost.json`：抓鬼持久化次数，目前用于判断是否超过 20 次。

## 交互习惯

- 用户更喜欢像素坐标，不喜欢只显示相对坐标。
- 点击底层必须带随机偏移和随机等待，不需要每步重复强调。
- 用户希望框选按钮范围后，脚本在范围内随机点，不要只用中心点加 drift。
- 修改步骤时应尽量一次展示所有可编辑项，并支持直接截图/框选，不要让用户先去模板库单独操作。
- 不要擅自改变已确认的判断逻辑。例如战斗判断以顶部“回合”模板为准，不要改成战斗面板判断。
- 用户很在意运行速度，点击后等待不要和下一步点击前等待重复叠加。

## 抓鬼流程现状

当前 `flows/钟馗抓鬼.json` 是 17 步：

1. 点击活动。
2. 点击日常活动。
3. 识别 `钟馗抓鬼识别文字.png`，按模板位置动态点击参加。
4. 关闭活动弹窗。这里等待较长，避免点击打断 4 秒飞行棋倒计时。
5. 等待并点击 `好的我帮你.png` 接任务。
6. 点击屏幕中间，清掉接任务后的弹框。
7. 等待并点击 `天眼通符.png`。
8. OCR 右侧任务栏，检测是否包含 `大唐境外` 或 `五庄观`。
9. 如果是特殊地图，点击飞行按钮开启飞行。
10. 点击右侧抓鬼坐标任务栏，自动寻路。
11. 等待 `抓鬼战斗按钮.png` 出现，在固定按钮区域内点击进入战斗。
12. 等待 `战斗回合标志.png` 出现再消失，判断战斗完成；完成后 `zhongkui_ghost.completed += 1`。
13. 如果是特殊地图，点击飞行按钮关闭飞行。
14. 如果是特殊地图，点击飞行棋。
15. 如果是特殊地图，点击驿站点；点击后等待 `4.5-5.5s`。
16. 如果是特殊地图，点击右下角关闭背包。
17. 短随机等待，进入下一轮。

## 抓鬼第 3 步策略

- 用户当天超过 20 次后，钟馗抓鬼会从日常活动第一屏移动到下面。
- `state/zhongkui_ghost.json` 持久化次数，不随工具箱重启清零。
- 当前第 3 步逻辑：先在当前列表识别，没找到再滑动重试，不再一开始就滑。
- 原因：每日活动窗口有时会记住滚动位置，一上来先滑会把钟馗抓鬼滑过头。
- 点击参加不是固定点，而是识别 `钟馗抓鬼识别文字.png` 后，用 `match_click_region_offset` 相对模板位置计算参加按钮区域。

## 特殊地图逻辑

- OCR 识别右侧任务栏，包含 `大唐境外` 或 `五庄观` 就设置 `fly_station_after_battle=true`。
- 这类任务去抓前开飞行，战斗结束后关飞行、点飞行棋、点驿站、关闭背包。
- 用户反馈：大唐境外走路很慢，五庄观也要经过大唐境外，所以都开飞行。
- 落地后不用额外等，关闭背包后直接从第一步继续。

## 抓鬼验证提醒

- 不自动处理验证，只检测疑似异常并走现有错误/暂停机制。
- 抓鬼第 12 步 `wait_battle_complete` 已增加回合 OCR 检测：
  - `round_ocr_region=[0.39,0.0,0.61,0.09]`
  - `round_ocr_interval=5`
  - `round_stall_alert_seconds=40`
  - `max_round_alert=5`
  - `battle_alert_seconds=125`
- 触发条件：同一回合超过约 40 秒未变化、进入第 5 回合、或战斗总时长超过 125 秒。
- 提醒方式：复用现有 `_handle_timeout(..., on_timeout=pause)`，保存截图、暂停流程、让工具箱按现有异常方式提示。暂不接微信，声音默认不加。
- 2026-08-11：抓鬼第 7 步 `天眼通符.png` 可能已经被系统自动使用，画面出现“你使用了天眼通符”后道具栏模板会消失。因此第 7 步 `on_timeout=continue`，找不到天眼不再暂停。

## 已知坑点

- 如果工具箱开着时直接改 JSON，旧窗口可能把流程文件覆盖回旧配置。改流程前最好先关闭工具箱，改完再打开。
- 2026-08-11 又确认一次：厢房等待参数曾被旧工具箱覆盖回旧值。以后改 `flows/*.json` 前，先查并关闭 `pythonw.exe *toolbox.py*`。
- ADB 有时 Windows 设备实例会卡成 Unknown。已有脚本：
  - `启用REDMI_ADB设备_管理员运行.ps1`
  - `清理REDMI_ADB设备_管理员运行.ps1`
- 失败时优先看 `logs` 最新尾部和 `failures` 最新截图。
- 抓鬼“关闭活动弹窗”曾因关闭按钮框选范围偏左/过大，随机点到顶栏边缘导致弹窗没关。当前第 4 步区域已收窄到关闭按钮核心区，后续若再失败优先看 `failures/debug_*_关闭活动弹窗_after_click.png`。
- `pythonw.exe` 通常会出现两个进程，是工具箱父子进程，正常。
- PowerShell 输出中文偶尔乱码，但文件本身是 UTF-8。

## 最近重要改动

- 工具箱改为深色主题。
- 左侧手机预览默认隐藏。
- 支持从任意步骤开始运行。
- 抓鬼超过 20 次后不再一开始就滑，改为先识别再滑动重试。
- 特殊地图后新增关闭背包步骤，防止下一轮活动被背包挡住。
- 战斗结束判断改为 `confirm_gone=1`，减少等待。
- 大量点击步骤的 `post_delay` 降低到 `0.05-0.15s`，避免点击后等待和下一步点击前等待叠加。
- 厢房战斗结束后等待已压短：`interval=3`、`confirm_gone=1`、战斗完成/清弹框后 `post_delay=0.05-0.15`、最后随机等待 `0.2-0.6`。
- 抓鬼超过 20 次后，点日常到列表下滑节奏已压快：第 3 步 `initial_delay=0.35-0.6`、`timeout_per_attempt=0.45`、`after_swipe_delay=0.45-0.75`。
- 抓鬼战斗按钮识别已压快：`抓鬼战斗按钮.png` 第一步等待从 `8-11s` 改为 `4.0-4.4s`，轮询间隔从 `2s` 改为 `0.35s`，成功后 debug 截图关闭。

## 下次可继续优化

- 2026-08-18：视频流路线记录，避免后续忘记：
  - 当前 `厢房_视频流测试.json` 使用的是 `screenrecord --output-format=h264 -` + PyAV 解码，只是过渡验证方案，不是最终目标。
  - 最终希望切到 `scrcpy 内部视频流`，原因是 scrcpy 的实时投屏链路更低延迟、更接近真实显示、更适合长期运行。
  - 不要直接把正式抓鬼/厢房/飞贼替换成 screenrecord 视频流；先保留测试流程观察稳定性。
  - 后续实现 scrcpy 内部视频流时，建议做成新的 FrameSource，例如 `ScrcpyInternalFrameSource`，继续通过流程级 `frame_source` 开关启用，先建 `抓鬼_scrcpy视频流测试`，不要污染正式流程。
  - 当前视频流基准：ADB raw 截图+模板约 1121ms；screenrecord 视频流缓存帧+模板约 203ms；任务栏 OCR 从约 1280ms 降到约 351ms。
- 把“框选点击范围”做成一等功能：框选多大就保存多大的点击区域，运行时直接在范围内随机点击。
- 编辑步骤时直接支持截图/框选坐标/框选模板，不要让用户跳到模板库。
- 给工具箱加一个“ADB 修复/重连”按钮。
- 给抓鬼第 3 步加 debug：保存每次滑动前后的活动列表截图，方便定位列表位置。
- 2026-08-11：成功路径 debug 截图改为后台单线程队列，最多排 2 张；队列忙时跳过，失败截图仍同步保存，避免影响稳定性。
- 2026-08-11：抓鬼战斗结束检测新增 `end_interval` 静默随机轮询；当前配置为 `0.75-1.25s`，战斗结束后额外等待 `0.03-0.10s`，目标是结束到下一轮活动约 1 秒多。
- 2026-08-11：五庄观开飞行曾点到飞行图标左上沿 `(1811,918)`，改为飞行/降落都点击翅膀图标核心区域 `x=1815-1858,y=935-982`，并启用区域随机点击和 after debug。
- 2026-08-11：OpenCV `!buf.empty()` 弹窗原因是 ADB 截图返回空/设备掉线。已在 `device.screenshot_png()` 加截图锁、PNG 校验和 3 次重试；`vision.py` 加 OpenCV 解码异常包装。
- 2026-08-12：抓鬼天眼后新增 `wait_text` 步骤，右侧任务栏需同时 OCR 到 `捉鬼` 和 `去` 才继续判断五庄观/大唐境外，避免任务栏未刷新时 OCR 到聊天飘字导致不开飞行。
- 2026-08-12：抓鬼进度 `zhongkui_ghost` 改为按日期保存/读取，跨日期自动当 0；当前状态已重置为 2026-08-12 completed=0，避免历史 97 导致未满 20 也下滑。
- 2026-08-12：工具箱流程页新增“设置抓鬼计数”按钮，可手动设置当天 `zhongkui_ghost.completed`；当前超过 20 场景已设为 20，允许第一屏找不到后下滑。
- 2026-08-12：修复“设置抓鬼计数”按钮无反应：原实现误用不存在的 `self.root_dir`；改为置前自定义弹窗，并在运行按钮旁显示当前抓鬼计数。
- 2026-08-12：抓鬼天眼后去掉 `wait_text` 阻塞等待；地图判断改为同步 OCR 一次且 `initial_delay=0`，随后固定点击任务栏，OCR 本身作为反应时间。
- 2026-08-12：修复厢房第 1 步活动按钮被误改为 `(747,40)`；恢复为活动按钮区域 `x=742-785,y=48-94`，并把厢房关闭活动弹窗收窄到关闭核心区。

- 2026-08-12: 厢房第一步活动坐标修正为 2608x1200 下的活动按钮范围 x742-785,y48-94；关闭活动弹窗改为范围 x2088-2142,y76-132。
- 2026-08-12: 新增 皇宫飞贼 流程模板；执行器支持 disabled 未配置步骤并在列表显示 未配置/跳过，框选/编辑后自动启用。

- 2026-08-12: 皇宫飞贼第8步取消抓鬼回合 OCR/回合停留/总战斗异常检测，只保留战斗标志模板进入与消失判断。

- 2026-08-12: 皇宫飞贼等待战斗结束 battle_timeout 从 180s 延长到 270s。

- 2026-08-12: 皇宫飞贼第7步从单点 x2279,y482 改为右侧任务栏范围点击 x2160-2500,y360-560，并开启点击后 debug 截图；pre_delay 调整为 0.2-0.45s。

- 2026-08-12: 修复工具箱加载流程时误保存的问题：_load_flow 只刷新列表不写文件；启动时自动关闭同项目旧工具箱进程，避免多窗口互相覆盖流程。恢复 皇宫飞贼.json 为 10 步流程。

- 2026-08-12: 修复流程 JSON BOM 读取问题：_load_flow 改用 utf-8-sig，并将 flows/*.json 重写为无 BOM UTF-8。

- 2026-08-12: 撤销工具箱启动时自动关闭旧进程逻辑，避免窗口误消失；皇宫飞贼第7步根据 debug 图从 x2160-2500,y360-560 下移缩窄为 x2160-2500,y430-545，避开右侧战斗按钮。

- 2026-08-12: 皇宫飞贼删除重复的领取后清理弹框2；任务栏自动寻路点击改为任务地点文字左半段 x2145-2375,y440-505。

- 2026-08-12: 皇宫飞贼从第一步重查：删除接任务后点中间清弹框；点飞行棋后等待 3.6-4.4s，点飞行点后等待 4.5-6.0s，NPC/领取任务加 debug 截图，避免背包遮挡任务栏。

- 2026-08-12: 工具箱简化为统一范围点击：隐藏/废弃单点坐标入口，新增 +范围点击；框选点击范围会保存 step_previews 图片并在修改弹窗显示；厢房第1步活动按钮恢复到 x742-785,y48-94。

- 2026-08-12: 厢房第1步改为 region_click_until_template：点击活动后验证 日常活动.png 出现，最多重试3次，解决活动按钮亮但未打开仍继续执行的问题。

- 2026-08-12: 撤回厢房第1步错误的 日常活动.png 校验；厢房打开活动后默认在其他任务页，日常活动模板不适合作为校验。

- 2026-08-12: 为解决活动按钮高亮但未打开，新增 tap_mode=swipe（1px/65ms 短滑点击）；厢房第1步活动按钮改用短滑点击并收窄到按钮中心。
